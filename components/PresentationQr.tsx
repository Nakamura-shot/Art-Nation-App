"use client";import {useEffect,useState} from "react";import QRCode from "qrcode";
export default function PresentationQr({code,title,location,expires}:{code:string;title:string;location:string;expires:string}){const [qr,setQr]=useState("");useEffect(()=>{QRCode.toDataURL(`${window.location.origin}/event-access/${code}`,{width:1000,margin:2}).then(setQr)},[code]);return <main className="qr-presentation"><div className="qr-present-brand">ART NATION CEBU</div><h1>Scan to open your painting guide</h1><h2>{title}</h2><p>{location}</p>{qr&&<img src={qr} alt="Workshop guide QR"/>}<strong>Open your camera and scan the QR</strong><small>Guide access automatically expires after the workshop.</small></main>}