import {notFound} from "next/navigation";import Header from "@/components/Header";import {rest,publicTechniqueMediaUrl,supabaseUrl} from "@/lib/supabase-rest";
function mediaUrl(v?:string|null){if(!v)return null;if(v.startsWith("http"))return v;if(v.startsWith("/"))return supabaseUrl(v);return publicTechniqueMediaUrl(v)||null}
export default async function Page({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const rows=await rest<any[]>(`techniques?select=id,title,slug,short_description,category,instructions,image_path,video_url,active&slug=eq.${encodeURIComponent(slug)}&active=eq.true&limit=1`,{},true);const t=rows[0];if(!t)return notFound();const image=publicTechniqueMediaUrl(t.image_path),video=mediaUrl(t.video_url);return <><Header/><main className="technique-public"><section className="technique-hero"><div><span className="eyebrow">ART NATION TECHNIQUE</span><h1>{t.title}</h1><p>{t.short_description}</p>{t.category&&<span className="pill">{t.category}</span>}</div>{image&&<img src={image} alt={t.title}/>}</section><section className="technique-content panel"><h2>How to do it</h2><div className="instruction-copy">{t.instructions}</div>{video&&(video.includes("youtube")||video.includes("vimeo")?<a className="button" href={video} target="_blank">Watch short video ↗</a>:<video controls preload="metadata" src={video}/>)}</section></main></>}