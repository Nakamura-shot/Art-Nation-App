import {NextResponse} from "next/server";
import {getAdminUserWithRefresh} from "@/lib/admin-auth";
import {rest} from "@/lib/supabase-rest";
export async function PATCH(req:Request,{params}:{params:Promise<{id:string}>}){
 try{if(!(await getAdminUserWithRefresh()))return NextResponse.json({error:"Unauthorized"},{status:401});const {id}=await params;const b=await req.json();await rest(`guide_steps?id=eq.${id}`,{method:"PATCH",headers:{Prefer:"return=minimal"},body:JSON.stringify({title:b.title,instructions:b.instructions||null,technique_id:b.technique_id||null,video_url:b.video_url||null,sort_order:Number(b.sort_order||0),step_no:Number(b.sort_order||0)})},true);return NextResponse.json({ok:true})}catch(e:any){return NextResponse.json({error:e.message||"Could not save step."},{status:500})}}
export async function DELETE(_:Request,{params}:{params:Promise<{id:string}>}){try{if(!(await getAdminUserWithRefresh()))return NextResponse.json({error:"Unauthorized"},{status:401});const {id}=await params;await rest(`guide_steps?id=eq.${id}`,{method:"DELETE",headers:{Prefer:"return=minimal"}},true);return NextResponse.json({ok:true})}catch(e:any){return NextResponse.json({error:e.message||"Could not delete step."},{status:500})}}