import {redirect} from "next/navigation";import AdminShell from "@/components/AdminShell";import TechniqueForm from "@/components/TechniqueForm";import {getAdminUser} from "@/lib/admin-auth";export default async function Page(){if(!(await getAdminUser()))redirect("/admin/login");return <AdminShell active="Techniques"><header className="admin-topbar"><div><h1>New technique</h1><p>Create a reusable skill for painting guides.</p></div></header><main className="admin-content narrow-library"><TechniqueForm/></main></AdminShell>}