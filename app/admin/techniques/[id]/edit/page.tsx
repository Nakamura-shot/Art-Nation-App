import {redirect,notFound} from "next/navigation";import AdminShell from "@/components/AdminShell";import TechniqueForm from "@/components/TechniqueForm";import {getAdminUser} from "@/lib/admin-auth";import {rest} from "@/lib/supabase-rest";export default async function Page({params}:{params:Promise<{id:string}>}){if(!(await getAdminUser()))redirect("/admin/login");const {id}=await params;const rows=await rest<any[]>(`techniques?select=*&id=eq.${id}&limit=1`,{},true);if(!rows[0])return notFound();return <AdminShell active="Techniques"><header className="admin-topbar"><div><h1>Edit technique</h1><p>Update reusable instructions and media.</p></div></header><main className="admin-content narrow-library"><TechniqueForm initial={rows[0]}/></main></AdminShell>}