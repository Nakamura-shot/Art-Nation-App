import {redirect,notFound} from "next/navigation";import {getAdminUser} from "@/lib/admin-auth";import {rest} from "@/lib/supabase-rest";import PresentationQr from "@/components/PresentationQr";
export default async function Page({params}:{params:Promise<{id:string}>}){if(!(await getAdminUser()))redirect("/admin/login");const {id}=await params;const rows=await rest<any[]>(`event_guide_access_codes?select=id,code,label,starts_at,expires_at,event_sessions!inner(events!inner(title),locations!inner(name))&id=eq.${encodeURIComponent(id)}&limit=1`,{},true);const c=rows[0];if(!c)return notFound();return <PresentationQr code={c.code} title={c.event_sessions.events.title} location={c.event_sessions.locations.name} expires={c.expires_at}/>;}