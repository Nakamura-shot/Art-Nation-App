import {redirect,notFound} from "next/navigation";import AdminShell from "@/components/AdminShell";import PaintingForm from "@/components/PaintingForm";import {getAdminUser} from "@/lib/admin-auth";import {rest} from "@/lib/supabase-rest";export default async function Page({params}:{params:Promise<{id:string}>}){if(!(await getAdminUser()))redirect("/admin/login");const {id}=await params;const rows=await rest<any[]>(`guides?select=*&id=eq.${id}&limit=1`,{},true);if(!rows[0])return notFound();return <AdminShell active="Painting Library"><header className="admin-topbar"><div><h1>Edit painting</h1><p>Update the reusable painting profile.</p></div></header><main className="admin-content narrow-library"><PaintingForm initial={rows[0]}/></main></AdminShell>}