import {redirect} from "next/navigation";import AdminShell from "@/components/AdminShell";import PaintingForm from "@/components/PaintingForm";import {getAdminUser} from "@/lib/admin-auth";export default async function Page(){if(!(await getAdminUser()))redirect("/admin/login");return <AdminShell active="Painting Library"><header className="admin-topbar"><div><h1>New painting</h1><p>Create the painting profile, then build its guide.</p></div></header><main className="admin-content narrow-library"><PaintingForm/></main></AdminShell>}